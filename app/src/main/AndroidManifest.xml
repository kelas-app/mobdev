<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_kelas_circle"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/ic_kelas_circle"
        android:supportsRtl="true"
        android:theme="@style/Theme.Capstone"
        tools:targetApi="31">
        <activity
            android:name=".view.chat.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".view.chat.DetailChat"
            android:exported="false" />
        <activity
            android:name=".view.home.DetailProductBaruActivity"
            android:exported="false"/>
        <activity
            android:name=".view.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".view.register.RegisterActivity"
            android:exported="false" />

        <activity android:name=".view.profile.sellerprofile.sellertab.addproduct.ProductActivity" />
        <activity android:name=".view.profile.sellerprofile.sellertab.editproduct.EditProductActivity" />

        <activity android:name=".view.profile.editprofile.EditProfileActivity" />

        <activity android:name=".view.profile.menjadipenjual.BecomeSellerActivity" />

        <activity
            android:name=".view.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>